<%- include('../partials/head', {title: 'User Dashboard'}) %>
<%- include('../partials/navbar') %>

<div class="container mt-5 mb-5">
    <%- include('../partials/massage') %>
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow border-0">
                <div class="card-header bg-success text-white py-3">
                    <h4 class="mb-0">User Dashboard</h4>
                </div>
                <div class="card-body p-4">
                    <h5 class="text-success mb-4">Hello : <%= email %></h5>
                    <div class="d-flex gap-3">
                        <a href="/users/update-password" class="btn btn-success">Update Password</a>
                        <a href="/logout" class="btn btn-danger">Logout</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('../partials/foot') %>